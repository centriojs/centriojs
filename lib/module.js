'use strict';

const _ = require('./mixin');

const installModule = moduleId => {

};
setGlobalVar( 'installModule', installModule );

const uninstallModule = moduleId => {

};
setGlobalVar( 'uninstallModule', uninstallModule );

const activateModule = moduleId => {

};
setGlobalVar( 'activateModule', activateModule );

const deactivateModule = moduleId => {

};
setGlobalVar( 'deactivateModule', deactivateModule );

const getActiveModules = () => {

};
setGlobalVar( 'getActiveModules', getActiveModules );